# AGENT.md - AIエージェント行動指針 (Agent Guidelines)

本リポジトリ（EPGDeck）で作業を行うすべてのAIアシスタント・自律エージェント（Antigravity等）は、以下の原則と行動指針を厳守してください。

---

## 1. オリジナル作者への敬意と自由な機能拡張 (Respect for Upstream & Freedom to Innovate)
- **フォークの前提と著作権の尊重**:
  - 本リポジトリは、オリジナル作者（[l3tnun 氏](https://github.com/l3tnun) による EPGStation）の優れた開発成果をフォークして開発されています。
  - 元の著作権表示、ライセンス表記（MIT License）、作者クレジットは維持してください。
- **大胆な改善・拡張の許容**:
  - 上流への敬意は保ちつつも、**大規模なリファクタリングや新機能の追加、仕様変更は積極的に許容**されます。既存の制約にとらわれず、EPGDeckとしての最適な設計・改善を推進してください。

---

## 2. 実運用ユーザーへの配慮・破壊的変更の規約 (User Care & Breaking Changes)
- **実運用ユーザーの存在**:
  - 本ソフトウェアは実際に運用しているエンドユーザーが存在します。安定性と継続的な運用可能性を最優先に考慮してください。
- **破壊的変更（Breaking Changes）の事前確認**:
  - DBスキーマ変更、設定ファイル（`config.yml` 等）のフォーマット変更、APIの非互換な変更など、既存環境に影響を与える破壊的変更を行う場合は、**必ず事前にユーザー（メンテナー）へ相談し、明示的な許可を得ること**。
- **マイグレーションの設計・提供**:
  - 破壊的変更やデータ構造の更新を伴う場合は、TypeORMのマイグレーション作成や設定・データの移行手段（スクリプト、移行手順ドキュメント等）まで責任を持ってセットで設計・実装すること。

---

## 3. 公開リポジトリにおけるセキュリティとプライバシー (Public Hygiene & Privacy)
- **機密情報・個人情報の完全排除**:
  - 本リポジトリは公開（Public）リポジトリです。個人情報（氏名、メールアドレス等）、各種トークン・認証情報、プライベートな内部IPアドレスやローカル環境固有の絶対パスなどがコード、ログ、コメント、コミット履歴に混入しないよう徹底すること。
- **個人ファイル・一時ファイルの混入防止**:
  - 開発者個人の作業用メモ、テストデータ、エディタ設定等の個人的なファイルがリポジトリに含まれないよう、`.gitignore` や `.dockerignore` を遵守しクリーンな状態を保つこと。

---

## 4. 実行環境の保護とコンテナ隔離 (Environment Safety & Container Isolation)
- **コンテナ内での動作・検証**:
  - ホストOSおよび開発者の稼働環境に影響を与えないよう、ビルド、テスト、動作検証などは極力 Docker コンテナ内で実施すること。
- **稼働中サービス・実データへの不干渉**:
  - ホスト上で動作している可能性のある実稼働サービス（録画サーバー、DB、チューナー等）や実データ（録画ファイル、設定ファイル実体）を誤って上書き・停止・削除しないよう、常に安全なコマンド操作を選択すること。

---

## 5. 品質保証とドキュメント同期 (Quality & Documentation)
- **ビルド・検証の徹底**:
  - 変更を加えた際は、サーバー（`npm run build-server`）およびクライアント（`npm run build-client`）のビルド、型チェック、Lintが正常に通ることを確認すること。
- **ドキュメント・設定サンプルの同期**:
  - 設定項目やAPIの変更・追加を行った場合は、関連するドキュメント（`docs/` 配下や `config/*.sample.yml`、`api.yml` 等）も漏れなく追従更新すること。
- **明確なコミットメッセージ**:
  - 変更内容と意図が明確に伝わるコミットメッセージ（プレフィックス `feat:`, `fix:`, `chore:`, `refactor:` 等）を作成すること。
