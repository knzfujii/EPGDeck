# AGENT.md - AIエージェント行動指針 (Agent Guidelines)

本リポジトリ（EPGDeck）で作業を行うすべての AI アシスタント・自律エージェントは、以下の原則を厳守してください。

---

## 1. 基本姿勢と技術思想 (Philosophy & Standards)
- **上流への敬意と自由な革新**:
  - EPGStation（l3tnun 氏）の著作権・クレジットを尊重しつつ、EPGDeck としての大胆な設計刷新・機能改善を積極的に推進する。
- **標準規格と長期保守性の重視**:
  - 過去の慣習や場当たり的な対症療法にとらわれず、Web 標準仕様やエコシステムで広く支持されている健全で正統な設計・技術スタックを優先的に選択する。
- **破壊的変更の事前合意**:
  - DB スキーマ、設定フォーマット、公開 API の非互換な変更など既存環境に影響する変更は、必ず事前にユーザーへ相談・確認する。

---

## 2. 実行環境とプロセス規約 (Environment & Execution)
- **環境の保護とランタイム管理**:
  - ホストシステムのグローバル設定を勝手に変更せず、プロジェクト指定のランタイム環境を適切に使用して実行する。
- **ログ常時監視**:
  - 実行時は `logs/` 配下（`Service/system.log`, `access.log`, `Operator/system.log` 等）を常時監視し、潜在エラーを即座に検知・対処する。

### サーバーの起動と停止
- **本番モード起動**: `npm start`（ビルド済みの `dist/index.js` を実行）
- **開発モード起動**: `npm run dev`（バックエンド nodemon + フロントエンド Vite の並列起動）
- **停止方法**: 起動したプロセス（タスク）に対して **`SIGINT`（Ctrl+C 相当）を送信** して停止する。
  エージェントが自身で起動したプロセスは、必ずそのタスクの管理機能（タスク kill 等）を使って停止すること。

### ⚠️ プロセス管理の禁止事項（厳守）
- **プロセス名やパターンによる一括 kill（`pkill node`, `pkill -f index.js`, `killall node` 等）は絶対に禁止。**
  ホストマシンでは EPGDeck 以外にも複数の Node.js プロセスが稼働している可能性があり、無関係なプロセスを巻き込んで停止させる重大事故につながる。
- 停止対象のプロセスが特定できない場合は、独自に推測せずユーザーに確認を求めること。
- プロセスの起動・終了は標準シグナル（`SIGINT`, `SIGTERM`）によるグレースフルシャットダウンに準拠し、不要なバックグラウンド残存プロセスを発生させない。

---

## 3. 品質保証・Git・公開規約 (Quality, Git & Hygiene)
- **コミット前の一括チェック徹底（最重要）**:
  - コミット前には必ず **`npm run check`** を実行し、全項目（Linter, Formatter, 型チェック, 単体テスト, ビルド）のエラーゼロ（100% PASS）を確認する。
  - 設計・仕様を変更した際は `docs/` 配下のドキュメントを同期更新する。
- **コミット前のフォーマッタ先行実行の徹底**:
  - コミット時にフォーマット差分が残らないよう、必ず事前に `npm run format` を実行して整形済みの状態でステージング・コミットを行う。
- **コミット規約とセキュリティ**:
  - 変更内容と意図が明確に伝わるコミットメッセージ（`feat:`, `fix:`, `refactor:`, `chore:` 等）を記述する。
  - 公開（Public）リポジトリのため、個人情報・認証情報・内部パス・作業用一時ファイルの混入を徹底排除する。

---

## 4. プロジェクト固有の技術仕様と動作原則 (EPGDeck Technical Knowledge)

### 4.1. 動画再生・ストリーミング・字幕 (Streaming & Video Player)
- **配信方式ごとの特性とシーク・時間制御**:
  - **直接再生（MP4等）**: ファイル実尺が確定しているため、ブラウザネイティブの `videoElement.duration` を最優先でシークバーに反映する。
  - **HLS / WebM トランスコード配信**: ブラウザ側は開始直後、全体の尺が不定（`Infinity` または初期セグメント数秒）になるため、DB に記録された実録画時間（`totalDuration`）を初期値として同期する。
  - **字幕（ARIB STD-B24）**: 字幕データは ID3 Timed Metadata に変換される **HLS 配信でのみ完全対応**。M2TS-LL や WebM などの他形式では字幕ボタンを非表示・適切に制御する。
- **Svelte 5 `$props()` とリアクティビティ**:
  - `$props()` の分割代入（destructuring）でローカル変数に展開すると、親からの動的な値変更に対してリアクティビティが途切れることがあるため、`props.xxx` を直接 `$derived` 内で参照する。
  - トグルボタン等の UI ハイライトは、コンポーネント内のローカル `$state` と連動させて即時描画更新を保証する。

### 4.2. 録画管理・時間計算 (Recording & Duration Management)
- **実録画時間（実尺）の記録と保存**:
  - 録画データ（`recorded`）の `duration` は「予定放送枠」ではなく「実際にチューナーからデータを受信開始した時刻〜終了した時刻の実測ミリ秒」を保存・反映する（途中録画・手動停止・ドロップ時でも実尺と整合性を維持する）。

### 4.3. API 通信とパフォーマンス最適化 (API & DX Performance)
- **直列 await（ブロッキング）の排除**:
  - 画面遷移時（`RecordedDetail` 等）に複数の API を直列待機せず、`Promise.all` による並列フェッチや、設定モーダル用等の補助データはバックグラウンド非同期取得にしてメイン画面の描画ブロックを防止する。


