# AGENT.md - AIエージェント行動指針 (Agent Guidelines)

本リポジトリ（EPGDeck）で作業を行うすべての AI アシスタント・自律エージェントは、以下の原則を厳守すること。

---

## 1. 基本姿勢・技術思想
- **上流への敬意と革新**: EPGStation（l3tnun 氏）の著作権を尊重しつつ、EPGDeck としての大胆な設計刷新・機能改善を推進。
- **一見奇妙な実装の背景調査原則**: 上流から引き継いだコードに一見不自然な処理（例: 重複調停キーにステータスフラグが含まれる等）があっても、安易に「バグ」と決めつけてリファクタリングしないこと。必ず `git log -S` やコミット履歴、過去の Issue（例: #517）を精査し、過去の障害回避・エッジケース救済の設計意図を解明した上で判断する。
- **仮説と実データの検証**: 仕様上の懸念（例: 角括弧除去による話数誤爆など）が生じた際は、コードの字面だけで推測せず、日本の放送規格（ARIB外字・表記実態）や本番データベースの実データを突き合わせて客観的に影響度を評価する。
- **標準規格重視**: 慣習にとらわれず、Web 標準と健全なエコシステム技術スタックを優先。
- **王道・標準的な設計・実装の徹底（Standard & Idiomatic Practices）**:
  - 長期的なメンテナンス性と可読性を最優先とし、アドホックな回避策（インラインのハードコード設定注入、力技のモック、場当たり的な一時ハック）を避ける。
  - フレームワークやエコシステム（Node.js, TypeScript, Playwright, Drizzle, Hono 等）が提供する標準的・王道なプラクティス（専用設定ファイルによる明示的構成、標準環境変数、公式推奨フィクスチャ等）を強く推奨する。
  - 独自仕様や車輪の再発明を排し、初見の開発者や後続のエージェントが迷わず理解・保守できる、シンプルで予測可能性の高い設計を維持すること。
- **破壊的変更の事前合意**: DB スキーマ、設定形式、公開 API 変更など既存環境に影響する変更は必ず事前にユーザー承認を得る。

---

## 2. 実行環境とプロセス管理
- **環境保護**: ホスト設定を勝手に変更せず、指定ランタイム（`mise` 経由 Node.js）を使用。
- **ログ常時監視**: `logs/epgdeck.log` を監視し潜在エラーを検知・対処。
- **起動と停止**:
  - 本番: `npm start` / 開発: `npm run dev`
  - 停止: 起動したタスクへ `SIGINT`（Ctrl+C）を送信。管理機能で確実に停止する。
- **⚠️ 一括 kill の絶対禁止**:
  - `pkill node`, `pkill -f index.js`, `killall node` 等はホストの他プロセスを巻き込むため厳禁。プロセス特定不能時はユーザーに確認。

---

## 3. 品質保証・テスト・Git 規約

### 3.1. テスト戦略と実行ルール
- **テスト同封と継続保守**:
  - 機能追加・バグ修正時は必ず対応するテスト（単体／結合／E2E）を新規作成または追記する。
  - 不具合再発防止のため再現・境界値ケースを固定し、仕様変更時は skip せずテストを正しく更新する。
- **テストの独立性**:
  - `beforeEach` / `afterEach` で状態をクリーンアップし、実行順序に依存しない冪等なテストにする。
- **スコープに応じたテスト実行**:
  - **バックエンド単体テスト (`npm test`)**: SQLite インメモリによる超高速実行（約1.5秒）を維持し、ロジック変更時に手元で手軽に検証する。
  - **クライアント純粋ロジックの単体テスト (`test/client/`)**:
    - コンポーネント全体の描画・E2Eテストとは別に、UI 内で利用される計算ロジック（番組表の座標計算、日付判定、文字列変換等）は `client/src/lib/utils/` に純粋関数として集約し、`test/client/` 配下で Vitest により高速・網羅的に境界値テストを実施すること。
  - **画面・UI変更時の E2E 検証 (`npm run test:e2e`)**:
    - `client/` の画面・コンポーネント・ルーティング・フォーム・UI文言に変更を加えた場合は、**コミット前に必ずローカルで Playwright E2E テストを実行して検証すること**（`npm test` のパスだけで画面の安全性を判断しない）。
    - 変更範囲が限定的な場合は、対象スペックを指定して高速に検証可能（例: `npx playwright test test/e2e/rule_edit.spec.ts`）。
  - **DB 結合テスト (`npm run test:mysql`)**: MySQL / MariaDB 結合テストは `test/integration/mysql.test.ts` に集約し、DB方言やクエリ層の変更時に `TEST_MYSQL=true` を付与してオプトインで検証する。
- **E2E テストのロケータ設計原則**:
  - Tailwind CSS のユーティリティクラス（`class="..."`）による要素取得は、スタイル微調整やリファクタリングで極めて壊れやすいため避ける。
  - 原則として Playwright のベストプラクティスに従い、アクセシビリティ要素（`getByRole`, `getByLabel`）または `getByTestId`（`data-testid`）を用いる。
  - `getByRole('button', { name: ... })` などで要素を取得する際は、サブテキスト追加等の軽微なUI改修でテストが落ちないよう、末尾括弧等の過度な厳密一致を避け、部分一致（柔軟な正規表現等）で指定する。
- **スタンドアロン E2E サーバー構造の維持**:
  - Playwright E2E テストは `test/e2e/e2e_server.ts`（スタンドアロン Hono サーバー）と連携させ、Mirakurun が稼働していない環境（CIやローカル初期環境）でも自律して動作する構造を維持すること。
- **テスト用データベースの完全分離と命名原則**:
  - テスト実行時は、本番・開発用データベース（`data/database.db` や実運用の MySQL）および `config/config.yml` を絶対に参照・変更しないこと。
  - 各テストは必ずテスト目的が明確にわかる名前のデータベース（テスト名が付いた DB）を使用すること：
    - E2E テスト: `data/test_e2e.db`（`test/e2e/e2e_server.ts` 内で完全分離し、`data/test_e2e_env/` で完全独立稼働）
    - 単体テスト: `:memory:`（インメモリ）または `test_*.db`（例: `test_program_db.db`, `test_drizzle_helper.db`）
    - MySQL 結合テスト: Docker 等のテスト専用コンテナ（デフォルト: 13306 ポート）
  - E2E テストサーバー起動時は、手元の `config/config.yml` を読み込まず、E2E 専用の設定ファイル（`config/config.test.yml`）を読み込んで自律起動すること。

### 3.2. コード品質・型整合性・CI/CD
- **非同期処理（Promise）の厳格な管理**:
  - **Floating Promise の排除 (`no-floating-promises`)**: Promise を返す非同期関数を呼び出す際は、必ず `await` するか、意図的なバックグラウンド実行・イベントハンドラ内発火である場合は `void fn()` で明示的に浮遊 Promise を排除すること。
  - **async Promise executor の禁止 (`no-async-promise-executor`)**: `new Promise(async (resolve, reject) => { ... })` は例外捕捉漏れ（キャッチされない拒絶）の原因となるため厳禁。同期的な executor 内でハンドリングするか、外側の `async` 関数として実装すること。
- **ESLint 準拠と未使用変数の命名規約**:
  - `@typescript-eslint/no-unused-vars` に従い、引数や分割代入で意図的に使用しない変数には必ず `_` プレフィックス（例: `_req`, `_status`）を付与すること。テストコード等で CommonJS の require を使う必要がある場合も、ESM 準拠または `createRequire(__filename)` を用いること。
- **型整合性（Type Integrity）**:
  - `api.d.ts` 等の型変更時は、クライアント（`client/`）とサーバー（`src/`）双方が一致することを `npm run check`（`tsc` & `svelte-check`）で確認する。
- **コミット前チェックとフォーマット先行実行**:
  - コミット前に必ず `npm run format` を先行実行し、コード差分が整形済みの状態でステージング・コミットを行う。
  - `npm run check`（Linter, 型チェック, ビルド）の 100% PASS を確認すること。
  - コミットメッセージは規約（`feat:`, `fix:`, `refactor:`, `test:`, `docs:` 等）に準拠し、一時ファイルや機密情報の混入を徹底排除する。
  - 設計・仕様変更や技術的知見・成果は `docs/` 配下の専門ドキュメント（`architecture.md`, `testing.md`, `epgdeck_change_spec.md` 等）に体系的に記録・更新し、完了したタスクは `docs/TODO.md`（未来のタスク管理ボード）から適切に整理・削除する。
- **CI/CD のコスト効率と無駄の排除**:
  - GitHub Actions のジョブ設計時は、仮想マシン（VM）の起動オーバーヘッド（約20〜30秒）と分単位の切り上げ課金を常に意識すること。
  - 数秒で終わるタスクを無闇に別ジョブ（別スレッド）へ分割せず、1つのジョブに統合して環境と成果物を使い回し、ランナー消費枠を最小化すること。
  - ドキュメント変更時の CI スキップ（`paths-ignore`）および連続プッシュ時の古いジョブ自動キャンセル（`concurrency`）を維持すること。

---

## 4. プロジェクト固有の技術仕様・動作原則

### 4.1. 動画・字幕・Svelte 5
- **シーク・尺制御**:
  - MP4等直接再生: ネイティブ `videoElement.duration` を最優先。
  - HLS/WebM 配信: 開始直後の尺不定対策として DB の実録画時間（`totalDuration`）を初期値に同期。
- **字幕配信**:
  - HLS: ARIB B24 を ID3 Timed Metadata に注入（リアルタイムトランスコード時）。
  - MP4直接再生: 動画内字幕（mov_text等）を `GET /api/videos/:id/vtt` がオンデマンドで WebVTT 抽出し `<track>` 表示。
- **MP4 字幕保持クラッシュ防止**:
  - ARIB 字幕の未定 duration による MP4 muxer 停止（`error -22`）防止のため、入力前オプションに必ず `-fix_sub_duration` を付与。
  - 字幕マッピング: `-map 0:s? -c:s mov_text -metadata:s:s:0 language=jpn`。
- **Svelte 5 リアクティビティ**:
  - `$props()` の分割代入による値更新の脱落を防止するため、`props.xxx` を直接 `$derived` 内で参照。
  - ボタン等のハイライトはローカル `$state` と連動させて即時描画を保証。

### 4.2. 録画管理・通信・派生データ
- **実録画時間（実尺）の保存**:
  - `recorded.duration` は予定枠ではなく「チューナー受信開始〜終了の実測ミリ秒」を保存（途中終了・ドロップ時も実尺と一致）。
- **非ブロッキング通信**:
  - 画面遷移時に直列 `await` で待機せず、`Promise.all` 並列取得や補助データのバックグラウンド非同期取得で描画ブロックを排除。
- **ライフサイクル（オンデマンド vs 永続化）**:
  - 字幕 (WebVTT): 視聴時のみ単発要求のため、ディスク保存せず FFmpeg pipe ＋ LRU インメモリキャッシュ（ファイルレス）で配信。
  - サムネイル: 一覧表示で大量アクセスされるため、ディスク永続化して高速配信（録画削除時の連動削除を徹底）。
- **二重録画防止（録画履歴）の原則**:
  - `recorded_history` は軽量テキストデータのため、ユーザーの意図した無期限（`0`）または長期保持を優先し、不用意に短期自動削除しない。
  - 録画失敗時は再放送の重複スキップ防止のため履歴に残さず、正常終了時のみ保存する。
  - 手動の個別予約であっても、ユーザーの「録画済み」の意図を尊重して履歴に保存する。
- **予約重複調停（同一番組の複数ルール）の原則**:
  - 同一番組（`programId`）に対する調停は、優先度順（手動 ＞ ルール優先度）で先にインデックス化され、ステータス（スキップ/通常）ごとに独立性を保つこと（別ルールでの救済録画を可能にする）。

### 4.3. 番組表（EPG）の時間軸・描画・計算仕様
- **4:00 起点の日付境界と深夜帯（0:00〜3:59）の基準日判定**:
  - 日本のテレビ放送・番組表（ARIB/EPG規格）では、1日の区切りは「朝4:00〜翌朝4:00」である。
  - 深夜帯（0:00〜3:59）は暦日（カレンダー日）では翌日だが、番組表上は「前日のタイムライン」に属する。
  - 単純な `toDateString()` 等のカレンダー日付一致で比較すると、深夜帯に「前日」と判定され、自動スクロールや当日判定が破綻する。日付・表示判定は基準日算出（`getBaseDate()`）や表示範囲判定（`guideStartAt <= now && now < guideEndAt`）を用いて厳格に行うこと。
- **局名ヘッダー（48px）のオフセットと現在時刻線のアライメント**:
  - 番組表グリッドにおいて、番組セルや時間目盛り列は局名ヘッダー（`HEADER_HEIGHT = 48px` = `h-12`）の直下を `top: 0` の基準として配置される。
  - 局名ヘッダーを含む親コンテナ直下に配置されるオーバーレイ（現在時刻の赤線インジケーターなど）は、局名ヘッダー分のオフセット（48px）を必ず加算して計算すること。
- **番組表ロジックの純粋関数化**:
  - スクロール位置、現在時刻線の位置、日時判定などの計算ロジックは Svelte コンポーネント内にインラインで記述せず、`client/src/lib/utils/guide.ts` のような独立した純粋関数として切り出し、境界値（03:59:59, 04:00:00 等）の単体テストを担保すること。
