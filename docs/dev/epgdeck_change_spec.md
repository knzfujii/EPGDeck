# EPGDeck (Svelte 5 版) 画面変更仕様書

本ドキュメントは、[EPGStation 画面仕様書 (オリジナル)](./epgstation_ui_spec.md) をベースとして、2026 年のモダン Web 技術（Svelte 5 + Vite 6 + Tailwind CSS v4）への移行、および **15,000 件以上の録画アーカイブ運用** を想定した UI/UX の最適化・統廃合・新機能仕様をまとめた変更設計書です。

---

## 1. 刷新の基本方針と設計思想

1. **仮想 DOM レスによる圧倒的な軽快性 (Svelte 5 Runes)**:
   - 従来の Vue 2 (仮想 DOM + 重厚な Vuetify) から Svelte 5 のネイティブ DOM コンパイルへ移行。
   - CSS サイズを **約 96% 削減 (1.1 MB ➔ 61 kB)**、JS バンドルを **約 76% 削減** し、モバイルや低スペック端末でも極めて高速に動作。
2. **15,000件超の録画アーカイブ特化**:
   - 従来のページ番号移動だけでは困難だった過去データへのアクセスを、**「年・月セレクター（年月ジャンプ）」** と **「ジャンルチップ絞り込み」** で瞬時に検索・閲覧可能に。
3. **動画再生アプリとしての導線強化**:
   - 目的である「番組を見る・再生する」を最優先とし、各画面で **ワンクリックで即座に再生できる目立つ導線（鮮やかな再生ボタン・サムネイルオーバーレイ）** を配置。
   - プレイヤー自体も映像鑑賞に最適な **シャープな四角（直角デザイン / `rounded-none`）** を採用。
4. **デザインシステム（デザイントークン）の完全統一**:
   - 全画面の垂直コンポーネント間余白を **`space-y-5` (20px)**、ヘッダーカードを **`p-4 sm:p-5`** に統一し、一貫性のあるリズム感と視認性を実現。

---

## 2. 画面一覧・統廃合対比表

| # | 画面名 | パス | オリジナル (EPGStation) | **EPGDeck (Svelte 5 版)** | 変更種別 |
| :---: | :--- | :--- | :--- | :--- | :---: |
| 1 | **ダッシュボード** | `/` | 3カラム（録画中/録画済/予約） | **「🔴録画中」「予約」「録画済」のサマリー ＋ 直近予約/最新録画の2カラム**。再生ボタンを直接配置。 | **最適化** |
| 2 | **放映中** | `/onair` | カード形式のチャンネル一覧 | **テーブル一覧（表形式）**。`[放送局/時間, 視聴, 現在の番組, 次の番組]` 構成。番組詳細ポップアップ ＆ 次番組ワンクリック予約完備。 | **改善** |
| 3 | **ライブ視聴** | `/onair/watch` | 単独ビュー | **高機能統合動画プレーヤー (`Watch.svelte` / `VideoPlayer.svelte`)**。四角デザイン、HLS キープアライブ & 離脱時の自動チューナー解放。 | **統合** |
| 4 | **番組表** | `/guide` | EPG グリッド | **チャンネル幅 70% 縮小 (`w-40` / 160px)**、個別セル開始時刻削除でタイトル・概要最大化、ダークモード専用の高輝度ジャンルカラー。 | **高速・最適化** |
| 5 | **予約一覧** | `/reserves` | 予約リスト | **キャンセル（取消）ボタン ＆ スキップ中解除（復活）ボタン** ＋ 行クリックでの予約詳細ポップアップモーダル。 | **強化** |
| 6 | **手動時間指定予約** | `/reserves/manual` | 複雑なフォーム | **HTML5 ネイティブの Datetime ピッカーによる直感的な手動予約**。 | **改善** |
| 7 | **録画中一覧** | `/recording` | 独立した一覧ページ | **ダッシュボード最上部に優先表示** ＋ ワンクリック追っかけ再生。 | **統合** |
| 8 | **録画済み一覧** | `/recorded` | カード/テーブル | **カード表示 ⇄ テーブル表示のシームレス切り替え** ＋ 15,000件対応「年月ジャンプ」＆「ジャンルチップ」 ＋ 保護トグル。 | **大幅強化** |
| 9 | **録画詳細** | `/recorded/detail?recordedId=:id` | 独立した詳細ページ | 正しいサムネイル画像表示、録画ファイル情報、個別エンコード追加モーダル。 | **強化** |
| 10 | **録画再生** | `/recorded/watch` | 専用ビュー | **統合動画プレーヤー (`Watch.svelte`)** で直接/HLS/トランスコードをシームレス再生（四角デザイン）。 | **統合** |
| 11 | **ルール管理** | `/rule` | カードリスト | **テーブル一覧 ＋ 実予約件数集計バッジ ＋ EPGStation 完全互換のルール作成・編集ページ**（保存先ストレージ・ディレクトリ・エンコード3系統）。 | **大幅強化** |
| 12 | **エンコード管理** | `/encode` | 実行中・待機リスト | **進行状況プログレス ＋ ワンタップキャンセル** のスッキリしたキュー管理。 | **スリム化** |
| 13 | **番組検索** | `/search` | 検索フォーム | **キーワード検索 ＋ 検索結果からワンクリックで詳細ルール作成へ連携**。 | **機能強化** |
| 14 | **ストレージ容量** | `/storages` | 容量プログレス | **複数ドライブ（マウントポイント）完全対応** ＋ 使用/空き/総容量の数値表示。 | **改善** |
| 15 | **設定** | `/settings` | 多数のタブ・低スペック対策 | **2026年基準のスリム化**（テーマ、半角表示、PWA、自動フォルダ振り分け、重複スキップ）。 | **スリム化** |

---

## 3. 各画面の詳細仕様

### 3.1 ダッシュボード (`/`)
- **録画中番組の最優先アラート**:
  - 録画が進行している場合、最上部にパルスアニメーション付きの「🔴 現在録画中」セクションを表示。
  - 各録画中番組に **「▶ 視聴する」** ボタンを配置し、追っかけ再生・ライブ視聴へ即座に誘導。
- **2 カラムサマリー**:
  - 「直近の予約」（競合警告・重複スキップバッジ付き）
  - 「最新の録画済み」（**「▶ 再生」** ボタン付き）

### 3.2 放映中 (`/onair`)
- **テーブル一覧スタイル**:
  - カラム構成: `['放送局 / 時間', '視聴', '現在の番組', '次の番組']`。
  - 放送時間直下に視覚的な進行バーを配置（% テキストは省きスッキリ表示）。
  - 現在番組・次番組をクリックで「番組詳細ポップアップ」を展開（ワンクリック予約やルール作成へ遷移）。
- **配信設定モーダル**:
  - 「視聴」ボタン押下時にモーダルが展開（ストリーム形式 `HLS`, `MP4`, `WebM` と画質選択）。

### 3.3 番組表 (`/guide`)
- **コンパクトなチャンネル列幅**:
  - `w-40`（160px、従来の約 70%）に最適化し、1画面で多数の放送波・チャンネルを同時に俯瞰。
- **タイトル ＆ 概要の最大化**:
  - 各セル内の開始時間表示を削除し、短時間の番組でもタイトルや概要文が最上段からしっかり読めるレイアウト。
- **鮮明なダークモードジャンルカラー**:
  - 黒背景に沈まない高輝度ボーダー（`-400`系）と 80% リッチティント背景（`dark:bg-*-950/80`）でスタイリッシュに発色。

### 3.4 予約一覧 (`/reserves`)
- **キャンセル・スキップ管理**:
  - 手動予約は「キャンセル（予約取消）」、ルール予約は「キャンセル（重複スキップ）」として即時反映。
  - スキップ中の番組には「復活（スキップ解除）」ボタンを配置。
  - 行クリックで詳細ポップアップが開き、番組情報や保存先、エンコード指定を確認・操作可能。

### 3.5 ルール管理 (`/rule`)
- **実予約数の集計バッジ表示**:
  - `/api/reserves` から各ルールの実予約数を取得し、`4 件` などの青色バッジで表示。
- **詳細ルール作成・編集ページ (`/rule/edit`)**:
  - モーダルではなく独立ページとして実装し、縦長レイアウトで全項目を一括編集可能。
  - **基本検索条件**:
    - **キーワード（任意）**: 未指定時は全番組が対象（ジャンルや時間帯のみでのルール作成可能）。キーワード入力時に対象（番組名/概要/詳細）が未選択の場合は、自動的に「番組名」「概要」を有効化して検索・保存。
    - **除外キーワード**: キーワードと同様に対象（番組名/概要/詳細）の自動補完に対応。
    - **曜日・時間帯の指定（`times`）**: 番組の長さの上段に配置。曜日選択（全曜日/平日/土日切替、視認性の高い統一青トーン＆ダークモード非選択時の落ち着いた暗色配色）＋開始時刻（0〜23時）・時間幅（1〜24時間）
    - 番組の長さ（最小/最大分）、**検索対象期間（`searchPeriods`：開始日時〜終了日時ピッカー）**
  - **詳細条件（アコーディオン）**:
    - ジャンル・サブジャンル絞り込み（一括選択・除外対応）
    - 放送波（GR/BS/CS/SKY）および放送局の個別指定優先モード
    - **録画ファイル名フォーマット（`recordedFormat`）**: 詳細条件内で個別ルールごとの命名形式を柔軟に指定
  - **未来の録画予定プレビュー & スキップ管理（詳細条件の直後）**:
    - 「録画予定を検索する」ボタンにより、現在の設定条件に合致する未来の放送番組（`POST /api/schedules/search`）をリアルタイム検索。
    - 予約データ（`GET /api/reserves`）と自動照合し、「録画予約中」「スキップ中」「競合中」「重複」「未予約」の状態バッジを表示。
    - 予約番組ごとに**「スキップ」および「スキップ解除」**をワンタップで直接トグル可能。
  - **予約設定**: 有効/無効、二重録画防止（重複回避日数 `periodToAvoidDuplicate` 指定対応）、末尾切れ許可、無料放送限定。
  - **保存先・エンコード設定**: 保存先ストレージ・サブディレクトリ、第1〜第3エンコードプリセット、元ファイル自動削除。
  - 検索画面からは検索条件をクエリパラメータで引き継いで新規作成ページへ遷移。

### 3.6 録画済み一覧 (`/recorded`) ＆ 録画詳細 (`/recorded/detail`)
- **15,000件最適化**:
  - 年月セレクター（`2026年` × `8月` など）とジャンルチップ絞り込み。
- **録画詳細画面**:
  - サムネイル画像表示（`/api/thumbnails/:id`）、録画ファイルサイズ、保護トグル、エンコード追加モーダル。

### 3.7 統合動画プレーヤー (`Watch.svelte` / `VideoPlayer.svelte`)
- **四角（直角）デザイン (`rounded-none`)**:
  - 余計な角丸を排し、映像鑑賞に最適なシャープな直角スクリーン。
- **HLS キープアライブ ＆ 自動チューナー解放**:
  - 10秒ごとのキープアライブ Ping と、画面離脱・ストリーム終了時の自動 DELETE によるチューナーの即時解放。

---

## 4. データ・状態管理アーキテクチャ

- **共通ストア (`client/src/lib/stores/`)**:
  - `channels.svelte.ts`: 放送局マスターキャッシュ（`channelId ➔ 局名/放送波` マッピング）
  - `playerState.svelte.ts`: 視聴レジューム再生位置の自動保存・復元
  - `snackbar.svelte.ts`: グローバルトースト通知
  - `theme.svelte.ts`: ダーク/ライトテーマ切り替え
  - `router.svelte.ts`: Svelte 5 Native Reactive SPA Router（HTML5 History モード完全連動）
